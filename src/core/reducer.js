function reducer(state = {}, action) {
  switch (action.type) {
    case 'ADD': {
      return {
        state
      };
    }
    case 'REMOVE': {
      return {
        state
      };
    }
  }

  return state;
}